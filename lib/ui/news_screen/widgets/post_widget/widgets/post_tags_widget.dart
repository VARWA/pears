import 'package:flutter/material.dart';

class PostTagsWidget extends StatelessWidget {
  const PostTagsWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
      child: const Text('Design, Figma, WebFlow'),
    );
  }
}